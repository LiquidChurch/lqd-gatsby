query PREVIEW_QUERY($id: ID!) {
  series(id: $id, idType: ID, asPreview: true) {
    SeriesImage {
      fieldGroupName
      image {
        id
        sourceUrl
      }
    }
    count
    databaseId
    description
    id
    link
    messages(first: 100) {
      nodes {
        id
      }
    }
    name
    slug
    taxonomy {
      node {
        id
      }
    }
    termGroupId
    termTaxonomyId
    uri
    __typename
  }
}
