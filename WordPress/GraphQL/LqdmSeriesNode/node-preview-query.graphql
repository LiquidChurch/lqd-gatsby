query PREVIEW_QUERY($id: ID!) {
  lqdmSeriesNode(id: $id, idType: ID, asPreview: true) {
    count
    databaseId
    description
    id
    link
    lqdmMessages(first: 100) {
      nodes {
        id
      }
    }
    name
    slug
    taxonomy {
      node {
        id
      }
    }
    termGroupId
    termTaxonomyId
    uri
    __typename
  }
}
