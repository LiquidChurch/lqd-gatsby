query NODE_LIST_QUERY($first: Int!, $after: String) {
  lqdmSeriesNodes(first: $first, after: $after, where: { parent: null }) {
    nodes {
      count
      databaseId
      description
      id
      link
      lqdmMessages(first: 100) {
        nodes {
          id
        }
      }
      name
      slug
      taxonomy {
        node {
          id
        }
      }
      termGroupId
      termTaxonomyId
      uri
      __typename
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
