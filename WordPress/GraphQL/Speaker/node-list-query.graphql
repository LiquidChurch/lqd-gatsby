query NODE_LIST_QUERY($first: Int!, $after: String) {
  speakers(first: $first, after: $after, where: { parent: null }) {
    nodes {
      count
      databaseId
      description
      id
      link
      messages(first: 100) {
        nodes {
          id
        }
      }
      name
      slug
      taxonomy {
        node {
          id
        }
      }
      termGroupId
      termTaxonomyId
      uri
      __typename
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
